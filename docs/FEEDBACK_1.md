1. LNCURL logo last ascii two lines are not aligned correctly
2. add a copy button for the CURL, make "$" not selectable
3. if the user adds a last word, include it in the example curl request
4. add a dice icon button on the right hand side of the epitaph input, which will set a random one
5. Community funding: the lightning addresses need to be from env variables
6. Feature bounties: remove "Leave Flowers", "ASCII animations", "Sound effects", "Death Notifs"
7. Add hover descriptions over TPS and VPS
8. In "Node Stats" Add a link to the node https://amboss.space/node/<node_pubkey> (node pubkey via .env)
9. generating lightning address logic is bad, it needs to try create the address at getalby.com, not based  
   on the local database. There is no guarantee that others won't take a domain.
